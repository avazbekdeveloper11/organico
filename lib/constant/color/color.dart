import 'package:flutter/rendering.dart';

bool mode = false;

Color textsColor = mode ? const Color(0xFFFFFFFF) : const Color(0xFF171725);
Color buttontextColor = !mode ? const Color(0xFFFFFFFF) : const Color(0xFF171725);
Color iconColor = mode ? const Color(0xFFFFFFFF) : const Color(0xFF92929D);
